# ほげ走

ブラウザだけで公開できる、ドット絵の横スクロール・ローグライクランゲームです。

## 入っている機能

- ユーザー名だけでログイン（裏側はSupabase Anonymous Sign-In）
- PC / スマホ対応
- ジャンプ：Space / W / ↑
- しゃがみ：S / ↓
- スマホ用 JUMP / DUCK ボタン
- キーバインド設定：ジャンプ3枠・しゃがみ2枠をゲーム画面から任意変更可能（ブラウザ保存）
- 野菜4種（にんじん・トマト・ブロッコリー・なす）
- ゾンビ
- しゃがみで避ける低空のお化け
- 紅茶の入ったカップ収集
- 10杯ごとにランダム3択の強化
- 二段ジャンプ / バネ靴 / シールド / ティーカップ磁石 / スロー / 当たり判定縮小 / 復活 / ティーセンサー
- 1プレイごとに強化がリセットされるローグライク方式
- Supabaseへの記録保存
- プレイヤーごとの自己ベストランキング TOP30
- サーバー時刻を使った簡易スコア検証
- 操作キャラを白うさぎメイドのスプライトに差し替え済み
- 待機モーション / しゃがみ専用アニメ / ジャンプ専用コマを実装
- ゲームオーバー時のキャラ演出を追加
- タイトルロゴをキャラに合わせてかわいく調整
- 画面配色をキャラクターに合わせたパステル基調へ調整
- npm / Node / ビルド作業不要
- GitHub Pagesでそのまま公開可能

---

# 1. Supabaseを準備する

1. https://supabase.com/ で新しいProjectを作成します。
2. Supabase Dashboard の **SQL Editor** を開きます。
3. このフォルダの `supabase.sql` の中身を全部コピーして実行します。
4. Authentication の設定で **Anonymous Sign-Ins** を有効にします。
5. Project URL と Publishable key を確認します。

> Anonymous Sign-Inを使うため、プレイヤーはメールアドレスやパスワードを入力しません。
> セッションはブラウザに保存されます。ブラウザデータを消したり別端末へ移ると別ユーザー扱いになります。

---

# 2. config.jsを書き換える

`config.js` を開いて、以下の2か所だけ変更します。

```js
window.HOGE_CONFIG = {
  supabaseUrl: "https://xxxxxxxx.supabase.co",
  supabaseKey: "sb_publishable_xxxxxxxxxxxxx"
};
```

**service_roleキーは絶対に入れないでください。**
ブラウザ側に置くのはPublishable keyです。

---

# 3. GitHubへ「ブラウザから」アップロードする

Gitコマンドは不要です。

1. GitHubを開く
2. **New repository** で新しいリポジトリを作成
3. リポジトリ画面で **Add file → Upload files**
4. このフォルダの中にあるファイルをすべてドラッグ＆ドロップ
   - `index.html`
   - `styles.css`
   - `game.js`
   - `config.js`
   - `supabase.sql`
   - `README.md`
   - `player-bunny-sprites.png`
   - `player-portrait.jpg`
   - `player-start.png`
   - `player-idle.png`
   - `player-jump.png`
   - `player-crouch.png`
   - `player-gameover.png`
5. 下部の **Commit changes** を押す

ZIPファイルそのものではなく、**ZIPを解凍した中身**をアップロードしてください。

---

# 4. GitHub Pagesで公開する

1. GitHubリポジトリの **Settings**
2. 左メニューの **Pages**
3. Build and deployment の Source を **Deploy from a branch**
4. Branch を **main**、Folder を **/(root)**
5. **Save**

公開URLが表示されたら完成です。

例：

```text
https://あなたのGitHubユーザー名.github.io/リポジトリ名/
```

---

# 5. 遊び方

### PC

初期設定：

- ジャンプ：`Space` / `W` / `↑`
- しゃがみ：`S` / `↓`

ゲーム画面の **KEY CONFIG / 操作キー設定** から各キーをクリックし、その後に割り当てたいキーを押すと変更できます。設定はブラウザに保存されます。

### スマホ

- ゲーム画面を**上フリック**：ジャンプ
- ゲーム画面を**下フリック**：しゃがみ（約0.65秒）
- 短いタップや横フリックでは操作しないため、誤操作しにくい仕様です。
- 画面下の `JUMP` / `DUCK` ボタンも引き続き使用できます。

### 敵

- 野菜・ゾンビ：基本的にジャンプで回避
- お化け：低空を飛んでくるのでしゃがんで回避

### 強化

紅茶の入ったカップを10杯集めるたび、ゲームが一時停止してランダムな強化が3つ表示されます。
1つ選ぶとゲーム再開。強化内容はプレイごとにリセットされます。

---

# 注意：ユーザー名だけログインについて

このゲームでは「簡単に遊べること」を優先して、パスワードを使いません。
内部的にはSupabaseの匿名ユーザーIDを使ってランキングを分けています。

そのため：

- 同じブラウザではセッションを維持できます。
- 別端末から同じ名前を入力すると、別ユーザーとして扱われます。
- 同じ表示名を複数人が使うこともできます。

将来「名前＋4桁PIN」などに変更すれば、端末を跨いだアカウント復旧も可能です。

---

# ファイル構成

```text
hoge-run/
├── index.html
├── styles.css
├── game.js
├── config.js
├── supabase.sql
├── player-bunny-sprites.png
├── player-portrait.jpg
├── player-start.png
├── player-idle.png
├── player-jump.png
├── player-crouch.png
├── player-gameover.png
└── README.md
```

敵・背景・紅茶カップはCanvasで描画しています。
操作キャラは、`player-bunny-sprites.png` のスプライトシートを使ってゲーム内へ組み込んでいます。
走行・待機・ジャンプ・しゃがみでアニメーションを切り替えています。
またログイン画面では `player-portrait.jpg`、開始演出やゲームオーバー演出では `player-start.png` や `player-gameover.png` などの補助画像を表示しています。

背景は、著作物をそのまま再現せず、"不思議なティーパーティーの国"をイメージしたオリジナルの紅茶ファンタジー風にしています。


## 最新差し替え

- 操作キャラのメインスプライトは、新しく生成した**全身がしっかり入った4×4の走行アニメーションシート**へ差し替え済みです。
- 走行時の見切れや崩れを防ぐため、スプライトシートのコマサイズとゲーム側の表示倍率も調整しています。


## v8 走行アニメーション強化

- メインキャラの走行モーションを**3倍以上のカット数**に増やして、より滑らかに再生するよう更新しました。
- 新しい走行素材は `assets/player-run-1.png` 〜 `assets/player-run-48.png` として格納しています。
- 参考用に、元の統合シートも `assets/player-run-sheet-48.png` として同梱しています。
- コード側も `game.js` を更新し、走行アニメを **48フレーム** 読み込むように変更しました。
- 画像パスは `assets/` フォルダ基準へ統一しています。


## v9 UI可読性調整

- 能力選択画面で、背景とカードが混ざって見えにくかったため、選択オーバーレイのぼかし・遮光を強化しました。
- 能力選択パネルの内側にやわらかい半透明の下地を追加し、カードの文字が読みやすくなるよう調整しています。


## v10 能力選択UIを背景一体型に変更

- 能力選択画面を、背景イラストの上に別UIを重ねる方式から、**背景素材そのものがUIとして機能する方式**へ変更しました。
- 新しい背景 `assets/ability-select-ui.png` を追加し、3枚のカード枠と中央下の決定ボタンに合わせてクリック領域を調整しています。
- 各能力ごとに、かわいい専用アイコン画像を追加しました。
- 強化選択は **カードを選択 → 下中央の「えらぶ」ボタンで決定** の流れになっています。


## v11 能力選択UIの仕上げ

- 能力名・Lv・説明・選択ラベルの位置を、背景素材のカード枠に合わせて微調整しました。
- カード出現時に軽いスライドイン、選択時にアイコンのバウンスとピンクの発光、チェックマーク表示を追加しました。
- 1枚選択すると他の2枚は少し彩度を落として縮小し、どれを選んでいるか分かりやすくしています。
- 選択後は下中央の「えらぶ」ボタンが脈動し、決定時に選択カードが一度強く発光してからゲームへ戻ります。
- 未選択カードは暗くしすぎず、別候補へ選び直せることが分かる明るさにしています。


## v12 背景横スクロール

- ゲーム中の `stage-bg.png` を固定表示から横スクロール表示へ変更しました。
- キャラクターが前進すると、背景が右から左へ流れるように動きます。
- 地面タイルより背景を遅く流すことで、奥行きのある横スクロール感を出しています。


## v13 ローディング画面・ログイン停止対策

- ゲーム素材の読み込み完了前にユーザー名画面が操作できてしまう問題を修正しました。
- 初回ロード中は、PC/スマホの操作説明を表示する専用ローディング画面を表示します。
- 右下に揺れる `Now Loading` と、メインキャラの48フレーム走行アニメーションを表示します。
- ユーザー名送信後の匿名認証・プロフィール保存中もローディング画面を表示します。
- Supabase通信が無期限に待ち続けないようタイムアウト処理を追加しました。


## v14 タイトルロゴと能力選択画面の調整

- タイトルロゴをテキスト表示ではなく `assets/title-logo.png` の実画像表示に変更しました。
- ローディング画面、上部タイトル、スタートオーバーレイで同じロゴ画像を使うよう統一しています。
- 能力選択背景は、枠を除いた背景専用画像 `assets/ability-select-bg.png` を新規追加し、それを使用するよう変更しました。
- 能力選択画面は 4:3 比率を維持しつつ、PC で 1 画面内に収まるように viewport 基準で自動縮小します。
- カード・説明文・決定ボタンの位置も新背景に合わせて再配置しています。


## v15 添付スプライト差し替えとUI最終調整

- 添付されたモーション素材をもとに、走行・ジャンプ・着地・しゃがみ用スプライトを `assets/` に差し替えました。
- 走行アニメーションは 8 コマで再設定し、ローディング右下の走行アニメも同じ新素材を使うよう更新しました。
- ジャンプ後の着地専用アニメーション（`player-land-1..3.png`）を追加し、コード側で着地直後だけ再生されるようにしました。
- タイトルロゴは画像読み込み方式に統一し、`title-logo.png` をローディング / 上部ヘッダー / スタート画面で表示するように修正しました。
- 能力選択画面は、枠なし背景 `ability-select-bg.png` を使用し、PCで 1 画面内に収まりやすいよう viewport 基準でリサイズするようにしました。
